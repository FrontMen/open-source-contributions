# *** Warning! Your custom domain is not using HTTPS yet ***
# These rules will change if you change your site's custom domains or HTTPS settings

# Redirect domain aliases to primary domain
http://open-source.frontmen.nl/* http://opensource.frontmen.nl/:splat 301!

# Optional: Redirect default Netlify subdomain to primary domain
http://distracted-leavitt-2f946e.netlify.com/* http://opensource.frontmen.nl/:splat 301!
